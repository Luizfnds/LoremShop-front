import { UserRegistryData } from './user-registry-data';

describe('UserRegistryData', () => {
  let component: UserRegistryData;
  it('should create an instance', () => {
    expect(component).toBeTruthy();
  });
});
